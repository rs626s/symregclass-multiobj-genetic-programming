from src.workflow import run_experiment         #Import the 'run_experiment' function from the 'workflow' module located in the 'src' package

#This block ensures that the code below only runs when this script is executed directly, and not when it is imported as a module in another script
if __name__ == "__main__":
    run_experiment()                            #Call the 'run_experiment' function to start the experiment process
